{
  "name": "site",
  "version": "1.0.0",
  "description": "",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js",
    "browser-sync": "browser-sync start --proxy 'localhost:3002' --files 'public/*, public/**/*'",
    "dev": "concurrently \"npm start\" \"npm run browser-sync\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.1",
    "nodemon": "^3.0.1"
  },
  "dependencies": {
    "browser-sync": "^2.29.3",
    "express": "^4.18.2"
  }
}
